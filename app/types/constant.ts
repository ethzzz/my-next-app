export enum Path {
    INDEX = '/',
    HOME = "/home",
    CONTENT = "/content",
    PAGE = "/page",
    USER = "/user",
    USER_LAYOUT = "/user/layout",
    CATEGORY = "/category/:category",
    SERVER_RENDER = "/server-render",
}